# Running the Backend
Always run the backend through uvicorn and ensure the virtual environment is activated before running.

Command to use (Windows):
`.\.venv\Scripts\activate; uvicorn main:app --reload --host 0.0.0.0 --port 8000`
